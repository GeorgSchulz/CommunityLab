auto eth0
iface eth0 inet static
        address {{ floating_ip }}
        netmask 255.255.255.255
        gateway 172.31.1.1
        pointopoint 172.31.1.1
        dns-nameservers {% for nameserver in dns_nameservers %}{{ nameserver }}{% if not loop.last %} {% endif %}{% endfor %}
